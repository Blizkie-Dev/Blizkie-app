PORT=3000
NODE_ENV=development

# JWT - generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this

# AES-256 encryption key for messages (32 bytes = 64 hex chars)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MESSAGE_ENCRYPTION_KEY=your-32-byte-hex-encryption-key-change-this

# Email (nodemailer SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=Blizkie <your@gmail.com>

# In dev mode, OTP codes are logged to console instead of sent
# Set to "true" to skip actual email/SMS sending
DEV_MODE=true

# SQLite database path
# For Railway: set to /data/blizkie.db (mount a Volume at /data in Railway dashboard)
# Leave empty for local dev (defaults to ./data/blizkie.db)
# DB_PATH=/data/blizkie.db

# Cloudflare R2 file storage
# Leave empty to use local disk (dev only)
# R2_ACCOUNT_ID=your-cloudflare-account-id
# R2_ACCESS_KEY_ID=your-r2-access-key-id
# R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
# R2_BUCKET=blizkie-uploads
# R2_PUBLIC_URL=https://pub-xxxx.r2.dev  (or your custom domain)
